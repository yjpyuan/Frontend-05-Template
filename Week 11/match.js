/**
 * 只使用浏览器DOM基础api实现判断一个元素是否跟一个选择器相匹配？
 * @date 2020-12-17
 * @param {any} selector
 * @param {any} element
 * @returns {any}
 */
function match(selector, element) {
  
}


match("div #id.class", document.getElementById('id'));